version: "1.0"

buildInfo:
  buildName: netshoot

sonar:
  qgName: netshoot
  projectName: netshoot
  blockerIssueThreshold: 50
  coverageThreshold: 0
  smellsCountThreshold: 5000
  unitTestSuccessThreshold: 0

jira:
  projectName: res
  assignee: ljohnson
  components: Platform
  product: VO
  createVersionObject: false
  description: "K8S netshoot helm chart update"

stages:
  security:
    branch: (?!x)x
    sonarRegex: (?!x)x
    owaspDependencyRegex: (?!x)x
  publish:
    artifact:
      docker:
        - build 8x8/netshoot:${ART_VERSION} .
        - push 8x8/netshoot:${ART_VERSION}
  cd:
    enabled: false